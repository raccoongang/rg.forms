"""Django app configuration for examples."""

from django.apps import AppConfig


class ExamplesConfig(AppConfig):
    """Django app config for examples."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "examples"
